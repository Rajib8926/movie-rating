import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: {
      light: "#a2aab8",
      main: "#a2aab8",
      dark: "#a2aab8",
      contrastText: "#fff",
    },
    secondary: {
      main: "#ffff",
    },
  },
});
